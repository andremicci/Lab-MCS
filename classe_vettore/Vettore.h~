#include <iostream>

class Vector{
 public:
  Vector(double x=0, double y=0, double z=0):m_v{x,y,z}{};
  double X();
  double Y();
  double Z();
  void X(double);
  void Y(double);
  void Z(double);

  Vector operator+(Vector);
  Vector operator-();
 private:
  double m_v[3];
};
